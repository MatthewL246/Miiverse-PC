<Window x:Class="Miiverse_PC.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="using:Microsoft.UI.Xaml.Controls" mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Button x:Name="backButton" Grid.Column="0" Click="GoBack" IsEnabled="false">
            Back
        </Button>
        <Button x:Name="forwardButton" Grid.Column="1" Click="GoForward" IsEnabled="false">
            Forward
        </Button>
        <Button x:Name="reloadButton" Grid.Column="2" Click="Reload">
            Reload
        </Button>
        <TextBox x:Name="addressBar" Grid.Column="3" />
        <Button x:Name="goButton" Grid.Column="4" Click="NavigateToUriAsync">
            Go!
        </Button>

        <controls:WebView2 x:Name="webView" Grid.Row="1" Grid.ColumnSpan="5" HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch" Loaded="SetupWebViewHandlersAsync"
            Source="http://portal.olv.pretendo.cc" />
    </Grid>
</Window>
